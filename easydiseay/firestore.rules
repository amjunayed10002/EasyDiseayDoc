rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // EasyDiseay uses the Firebase Admin SDK only on the server.
    // No browser/client should access the application database directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
