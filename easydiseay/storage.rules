rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // EasyDiseay uploads/reads managed assets through the server-side
    // Firebase Admin SDK and signed URLs.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
